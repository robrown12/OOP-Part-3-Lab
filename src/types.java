class Car extends Vehicle {
    public Car(String make, String model, String color, int year, int door) {
        super(make, model, color, year, door);
    }
}

class Van extends Vehicle {
    public Van(String make, String model, String color, int year, int door) {
        super(make, model, color, year, door);
    }
}

class Motorcycle extends Vehicle {
    public Motorcycle(String make, String model, String color, int year, int door) {
        super(make, model, color, year, door);
    }
}

class Truck extends Vehicle {
    public Truck(String make, String model, String color, int year, int door) {
        super(make, model, color, year, door);
    }
}
